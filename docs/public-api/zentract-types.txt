# zentract-types public API — supported surface (default features)
# (regenerated on every `cargo test` by zenutils-apidoc; ZEN_API_DOC=check verifies, =off skips).
# Encodings: crate-name prefix stripped; auto traits collapse to a
# count + exceptions; trait impls collapse to one roster line per
# type (method signatures live at the trait definition); blanket
# impls omitted; re-export duplicates annotated `[also: path]`.
# DO NOT EDIT BY HAND — commit regenerated changes with the code.
#
# files: zentract-types.txt 25 lines (supported surface) | zentract-types.features.txt 0 added (features: none) | zentract-types.internal.txt 5 lines (5 hidden + 0 excluded-feature)

## summary
#
#   pub modules                                 1
#   pub types (struct/enum/trait/alias)         3
#   pub consts/statics                          2
#   inherent methods                            2
#   struct fields                               3
#   enum variants                              10
#   trait roster entries (type × trait)        13
#   auto-trait-complete types                   3
#
# per-module pub lines:
#   (root)                           21

## items (21 lines)

pub mod zentract_types
#[repr(C)] pub enum DType
pub DType::F16 = 1
pub DType::F32 = 0
pub DType::I8 = 3
pub DType::U8 = 2
#[repr(i32)] pub enum ErrorCode
pub ErrorCode::AbiMismatch = -5
pub ErrorCode::InferenceFailed = -3
pub ErrorCode::InvalidHandle = -4
pub ErrorCode::InvalidModel = -1
pub ErrorCode::Ok = 0
pub ErrorCode::ShapeMismatch = -2
#[repr(C)] pub struct TensorMeta
pub TensorMeta::dtype: DType
pub TensorMeta::ndim: u32
pub TensorMeta::shape: [u64; 8]
pub fn TensorMeta::f32_shape(&[u64]) -> Self
pub fn TensorMeta::num_elements(&self) -> u64
pub const ABI_VERSION: u32
pub const MAX_NDIM: usize

## trait impls (3 types)

DType: Clone, Copy, Debug, Eq, PartialEq
ErrorCode: Clone, Copy, Debug, Eq, PartialEq
TensorMeta: Clone, Copy, Debug

## auto traits

3 types implement all of: Freeze, RefUnwindSafe, Send, Sync, Unpin, UnwindSafe
