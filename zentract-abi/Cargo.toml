[package]
name = "zentract-abi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "cdylib plugin that wraps tract-onnx behind a C ABI"

[lib]
crate-type = ["cdylib"]

[dependencies]
zentract-types = { path = "../zentract-types" }
tract-onnx = "0.22"
